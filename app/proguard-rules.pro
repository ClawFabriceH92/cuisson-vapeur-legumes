# Add project specific ProGuard rules here.
# By default, the flags in this file are appended to flags specified
# in /usr/local/android-sdk/tools/proguard/proguard-android.txt

# Room, Hilt and Compose all ship consumer ProGuard rules in their AARs,
# so no manual keep rules should be needed for them in a standard setup.
# This file is intentionally close to empty for v1 — revisit once release
# minification is actually exercised on a real build (not possible in this
# sandbox, see root README).
